# --- Rust ---
/target/
**/*.rs.bk

# Scratch output of a tuning session — the live view rewrites this every time
# you save. Rows get copied back into race.rs by hand, deliberately.
src/race.tuned.rs

# --- Godot 4+ ---
client/.godot/
*.translation
.import/

# Mono
.mono/
data_*/
mono_crash.*.json

# --- build output ---
# Godot web exports are ~36 MB of regenerable artifacts. They were tracked
# before the consolidation; `git rm --cached` untracked them without deleting
# anything from disk. History still has them at eddf481.
legacy/exports/
**/node_modules/

# --- OS ---
.DS_Store
Thumbs.db
